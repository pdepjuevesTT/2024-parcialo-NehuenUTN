import parcial.*

describe "tests" {

  test "puede comprar" {
    const lionel = new Credito(cuentaBancaria=100,limiteCompra=1000,cantidadCuotas=6,interes=20) 
    assert.that(lionel.pagar(100))
  }

}
